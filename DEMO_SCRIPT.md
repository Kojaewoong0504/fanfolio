# Fanfolio 1분 데모 스크립트

이 문서는 MVP의 핵심 가치를 1분 안에 보여주기 위한 시연 순서다. 시연 전
백엔드와 프론트엔드를 실행하고 `APP_ENV=test`에서 테스트 시드 데이터를 준비한다.

## 준비

```bash
curl -X POST http://localhost:8000/api/test/reset
curl -X POST http://localhost:8000/api/test/seed \
  -H 'Content-Type: application/json' \
  -d '{"scenario":"core"}'
```

시연에 사용하는 계정과 코드는 테스트 환경 전용이다.

- 팬 로그인 토큰: `test-magic-link-fan`
- 관리자 로그인 토큰: `test-magic-link-admin`
- 아티스트 로그인 토큰: `test-magic-link-artist`
- 카드 코드: `NOVA-VALID-01`

## 60초 진행 대본

### 0–10초 · 서비스 소개

팬이 실물 카드에 인쇄된 코드나 QR을 등록하면, 단순한 이미지가 아니라 공식
디지털 포토카드가 컬렉션에 저장되는 서비스라고 소개한다.

### 10–25초 · 팬 카드 등록

1. 팬 앱에서 로그인한다.
2. 최초 설정에서 `드림스케이프`와 `유나`를 선택한다.
3. `+ 카드 등록`을 누르고 코드 `NOVA-VALID-01`을 입력하거나 QR을 스캔한다.
4. 발급된 카드의 공식 배지와 발행 번호를 확인한다.

핵심 멘트: “팬이 카드 정보를 직접 입력하는 것이 아니라, 서버가 검증한 1회성
코드로 공식 카드를 발급합니다.”

### 25–35초 · 컬렉션과 카드 상세

`컬렉션으로 이동`을 눌러 보유 수와 빈 슬롯을 보여준 뒤 카드를 열어 이미지,
획득 경로, 발행 번호, 손글씨·보이스 특전을 확인한다.

핵심 멘트: “카드의 소유권은 팬별로 분리되어 다른 팬의 카드 상세는 열 수 없습니다.”

### 35–48초 · 아티스트 스튜디오

1. 아티스트 스튜디오에서 카드 이미지와 카드명을 등록한다.
2. 손글씨 캔버스에 메시지를 쓰거나 손글씨 이미지를 업로드한다.
3. `배경 제거 요청`으로 투명 PNG를 만들고 위치·크기·회전을 조정한다.
4. 미리보기에서 확인한 뒤 검수 요청을 보낸다.

핵심 멘트: “아티스트는 특별 카드를 만들 수 있지만, 팬에게 공개하는 권한은
관리자 검수를 거쳐야 합니다.”

### 48–60초 · 관리자 검수와 공개

1. 관리자 웹에서 `카드 관리`로 이동한다.
2. 대기 중인 카드를 검수 승인하고 `공개`한다.
3. `드롭·코드 관리`에서 발급 코드 배치와 드롭 상태를 관리한다.
4. 대시보드의 최근 활동에서 공개 기록을 확인한다.

마무리 멘트: “Fanfolio는 아티스트 제작, 관리자 검수·발급, 팬 수집을 하나의
공식 디지털 카드 흐름으로 연결합니다.”

## 시연 후 검증

전체 브라우저 흐름을 자동으로 재현하려면 다음 명령을 실행한다.

```bash
./scripts/e2e-smoke.sh
```

이 스크립트는 아티스트 카드 생성·손글씨 처리, 관리자 검수·게시, 코드 발급,
팬 QR 수집·상세 조회, 세 앱의 세션 범위를 함께 검증한다.
