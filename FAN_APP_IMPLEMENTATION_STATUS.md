# Fanfolio 구현 상태

이 문서는 `fanfolio-ui-review.html`의 시안과 현재 코드의 범위를 구분하기 위한 기준표입니다. 리뷰 HTML은 동작하는 화면이 아니라 역할별 화면 시안 모음이므로, 시안에 보이는 모든 픽셀이 API와 연결된 것은 아닙니다.

## 팬 앱

| 리뷰 보드 기능 | 현재 상태 | 구현 위치 |
| --- | --- | --- |
| 로그인·회원가입 | 구현 | `frontend/src/App.tsx`, `/api/auth/*` |
| 최초 설정 | 구현 | 아티스트 검색 → 멤버 선택 → 닉네임, `/api/onboarding` |
| 홈 | 구현 | 최근 카드, 컬렉션 요약, 카드 탐색 진입 |
| 컬렉션 | 구현 | 필터, 그리드/목록 전환, 미수집 슬롯, 완성 특전 |
| 카드 등록 | 구현 | QR 스캔, 코드 입력, 사진에서 QR 읽기 |
| 카드 공개 | 구현 | 등록 직후 공개 전환 화면과 특전 표시 |
| 카드 상세 | 구현 | 소유 카드 메타데이터, 발행번호, 획득 경로, 손글씨·보이스 |
| 탐색 | 구현 | 검색, 아티스트·멤버 필터, 정렬, 카드 상세 진입 |
| 알림 | 구현 | 전체/미읽음, 읽음 처리, 모두 읽음, 재시도 상태 |
| 설정 | 구현 | 프로필, 관심 아티스트·멤버, 계정, 이메일 알림, 언어·고객센터·약관·앱 정보, 로그아웃 |
| API 오류·로딩 상태 | 구현 | 한국어 오류 메시지, 재시도, 상세 조회 로딩 상태 |

로그인 직후에는 인증된 사용자 프로필을 다시 조회해 온보딩 여부를 확정합니다. 느린 초기 세션 요청이 신규 로그인 상태를 덮어쓰지 않도록 처리되어 있습니다.

## 별도 앱

| 리뷰 보드 기능 | 현재 상태 | 구현 위치 |
| --- | --- | --- |
| 관리자 로그인·대시보드 | 구현 | `admin_app/`, `/api/admin/*` |
| 관리자 카드·드롭·코드·사용자·감사 로그 운영 | 구현 | `admin_app/`, 관리자 계약 테스트 |
| 아티스트 로그인·스튜디오 홈 | 구현 | `builder_app/`, `/api/artist/*` |
| 특별 카드 제작·미리보기·임시 저장·검수 요청 | 구현 | `builder_app/app.js` |
| 손글씨 직접 입력 | 구현 | 캔버스 서명 패드 |
| 손글씨 이미지 업로드·배경 제거 작업 | 구현 | 업로드 presign + `/api/assets/{assetId}/background-removal` |
| 보이스 특전 업로드 | 구현 | 아티스트 업로드 흐름 |

## 아직 시안 수준인 부분

- `prototype-assets`의 이미지는 제품 데이터가 아니라 검수용 예시입니다. 실제 운영에서는 관리자/아티스트가 업로드한 이미지와 CDN URL을 사용합니다.
- 관리자 웹과 아티스트 스튜디오는 팬 앱과 같은 React 번들에 들어 있지 않고 각각 `admin_app`과 `builder_app`으로 분리되어 있습니다.
- 이메일 매직 링크는 현재 개발용 fixture 환경에서 동작합니다. 실제 메일 발송 provider와 배포 환경 설정은 별도 작업입니다.
- 손글씨 배경 제거는 작업을 등록하고 상태를 확인하는 MVP 흐름입니다. 실제 이미지 분리 품질과 비동기 워커 운영은 production hardening 대상입니다.

## 검증 기준

- 백엔드 계약 테스트: `131 passed, 2 skipped`
- 프론트 lint/build 통과
- 격리 포트 기준 전체 브라우저 스모크 통과: 신규 가입, 아티스트 스튜디오, 관리자 운영, QR 등록, 카드 공개·상세, 설정, 로그아웃, API 오류 표시
