# 2026-08-21 로컬 카드 서비스 시나리오 검증

## 환경

- 브라우저: Codex 내장 브라우저
- 팬앱: `http://127.0.0.1:5173`
- 관리자 웹: `http://127.0.0.1:4174/?api=http://127.0.0.1:8000/api`
- API: `http://127.0.0.1:8000`
- DB: `/private/tmp/fanfolio-task10.db` (기존 `backend/fanfolio.db`와 분리)

## 2026-08-21 검증 증거

- API `/api/health` 및 `/api/health/ready`: 200, healthy/ready
- 팬앱 `/`: 200
- 관리자 웹 `/`: 200
- 백엔드: `387 passed, 2 skipped`
- 팬앱: `143 passed`, build 성공, lint exit 0 (기존 경고 4건)
- 관리자 웹: `102 passed`
- 전체 역할·카드팩·특별 카드·QR/인증번호 계약 시나리오: 통과

## 통과한 흐름

1. 관리자 로그인 → 대시보드·카드·카드팩 실제 API 조회
2. 관리자 API로 카드팩 생성 → 확률 합계 100% → 공개
3. 팬 회원가입·온보딩 → 공개 카드팩 노출
4. 팬 카드팩 확률표 확인 → 카드팩 개봉
5. 새로고침 후 카드 컬렉션 `0 / 1 → 1 / 1` 반영
6. 카드 상세 독립 화면에서 아티스트 메시지·획득 경로·발행 수량 확인
7. 인증번호 유효 코드 등록 → 컬렉션 반영
8. 동일 인증번호 재등록 차단
9. 미공개 카드 인증번호 등록 차단
10. 관리자 신규 1회용 발급 배치 생성 → QR PNG 발급
11. 팬앱 실제 QR 인식 → 카드 확인 → 카드 공개 → 컬렉션 추가
12. 등록 완료 후 컬렉션 진행률 `1 / 9`와 카드 보상 표시

## 보류·실패가 아닌 검증 제한

- 관리자 카드 등록 UI는 이미지 첨부가 필수지만 Codex 브라우저 커넥터에서 파일 첨부를 수행할 수 없어 등록→검수→공개 UI 흐름을 완료하지 못했다.
- QR 카메라 프레임은 사용자가 직접 제공해 실제 인식까지 확인했다. Codex 브라우저 자동화만으로 카메라 프레임을 주입하는 방식은 별도 자동화 범위로 남긴다.
- 카드팩 생성·공개는 관리자 API로 검증했으며 관리자 UI mutation은 별도 검증 대상으로 남겼다.

## 판정

팬앱의 카드팩 개봉·인증번호 등록·실제 QR 등록 핵심 흐름은 로컬 실제 API에서 통과했다. Task 10 자동화·로컬 API 게이트는 통과했지만, 관리자 카드 등록 UI의 이미지 업로드→검수→공개 연속 흐름과 운영 환경 다중 기기 QR 호환성은 수동 검증이 남아 있어 프로덕션 배포 승인 게이트는 `검증 보류`다.
